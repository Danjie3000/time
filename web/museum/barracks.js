const guards = [
    "Hank the tank",
    "Thomas the tank engine",
    "Bert the tank",
    "CEPUS"
];

export function addGuards(guard) {
    guards.push(guard);
    return guards;
}

/*export default {
    guards,
    addGuards
};*/